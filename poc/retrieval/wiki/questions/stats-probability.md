# Stats + Probability

Source: google/questions/stats-probability.md

# Stats + Probability

Mapped from [PLAN.md](/Users/oribecher/Projects/private/job_interview/positions/google/PLAN.md): `Stats + Probability`.

## Glassdoor Questions

- `Q001` `(3x)` What is a p-value? Explain what a p-value means.
- `Q002` `(3x)` When would you consider mean over median?
- `Q003` `(3x)` What is the standard error of the mean?
- `Q004` `(2x)` For sample size n, the margin of error is 3. How many more samples do we need to make it 0.3?
- `Q005` `(2x)` What is the assumption of error in linear regression?
- `Q006` `(2x)` I have a deck and take one card at random. What is the probability you guess it right? Then given two questions (a) is it red? (b) is it 10 of spades? - which would you ask and why?
- `Q007` `(1x)` Given 3 coin tosses of a fair coin that all result in Heads - what is the probability the 4th is also Heads?
- `Q008` `(1x)` Given uniform distributions X and Y, mean 0, std 1 - what's the probability that 2X > Y?
- `Q009` `(1x)` There are 4 people in an elevator and 4 floors. What's the probability each person gets off on a different floor?
- `Q010` `(2x)` Make an unfair coin fair.
- `Q011` `(1x)` What is a random variable? How do you test if it comes from a particular distribution?
- `Q012` `(2x)` Explain a probability distribution that is not normal and how to apply it.
- `Q013` `(1x)` How can you tell if a given coin is biased?
- `Q014` `(1x)` Given a 1-point sample, design a test to check if the mean is 1 or not.
- `Q015` `(1x)` Is it good or bad to apply bootstrapping on samples to increase sample size?
- `Q016` `(1x)` How to implement the bootstrap?
- `Q017` `(1x)` Prove the probability integral transform.
- `Q018` `(1x)` Write code to generate iid draws from distribution X using only a random number generator.
- `Q019` `(2x)` Write a function to generate N samples from a normal distribution and plot the histogram.
- `Q020` `(1x)` Confidence interval calculation in Python.
- `Q021` `(1x)` Find the width of the confidence interval.
- `Q022` `(1x)` Question on the use of p-values in high-dimensional linear regression.
- `Q023` `(1x)` If the individual means of two groups are rising, is it possible that the pooled mean will decrease? If yes, how?
- `Q024` `(1x)` SE of sample mean and SE of sample median. Write a function to return elements from the Fibonacci sequence and analyze its computational complexity.
- `Q025` `(1x)` Standard error of median, interpret standard error of the mean. How to generate a matrix that follows Bernoulli distribution in Python; divide each element by sum of columns.
